# Changelog

All notable changes to this project will be documented in this file.
Generated by [git-cliff](https://github.com/orhun/git-cliff).

## [0.20.0] - 2026-08-25

### Added
- Canonical `sqlfy --version` output backed by package metadata (importlib.metadata)
- `sqlfyVersion` field in SchemaState JSON/YAML dump output
- `sqlfyVersion` field in manifest output (already present, now consistent)
- Tiered CLI reference documentation in README (Core, Analysis, Migration Management, Workflow, Experimental)
- API key documentation: separate Anthropic (text generation) and Voyage AI (embeddings) credentials

### Changed
- Reorganized CLI command table into tiered hierarchy for better discoverability
- Fixed CLI README: corrected test count (910), added lint optional dependency install instruction
- Marked `--embed` flag as experimental in documentation
- Updated lint command error message to use `pip install 'sqlfy-cli[lint]'`

### Fixed
- License presentation: README badge and text now consistently show GPL-3.0
- Version consistency: CLI `--version`, dump JSON/YAML, and manifest all report 0.20.0
- Documentation: removed stale source-line counts and outdated module references

### Technical
- Added `sqlfy_version` field to SchemaState dataclass and Pydantic model
- Regenerated Pydantic models from updated JSON schema (schema/types.json)
- Updated all test fixtures to include required `sqlfy_version` field
