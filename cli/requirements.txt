# sqlfy CLI — Python dependencies
# Currently stdlib-only. sqlglot will be added in step 8.

# Install runtime only:
#   pip install .
#
# Install in editable mode with dev tools (pytest):
#   pip install -e ".[dev]"
#
# Run tests:
#   python -m pytest -v
#
# Build a self-contained binary:
#   pip install pyinstaller
#   pyinstaller --onefile src/sqlfy/main.py --name sqlfy
# For binary builds:
#   pip install pyinstaller
# Optional — for embedding-based retrieval (step 15)
# voyageai  # only needed with --embed flag

sqlglot>=25.0.0